{
  "name": "escala-louvor-pib-maracaju",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "start": "node server.mjs",
    "preview": "vite preview --host 0.0.0.0",
    "check": "node --check app.js && vite build",
    "db:seed": "node scripts/gerar-seed-supabase.mjs"
  },
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "devDependencies": {
    "vite": "^7.1.1"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.112.3"
  }
}
